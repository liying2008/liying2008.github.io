spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/jpatest?charset=utf8mb4&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL55Dialect
debug: true
